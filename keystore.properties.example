# Copy to keystore.properties locally or create it in GitHub Actions from repository secrets.
# DO NOT commit keystore.properties or the .jks file.
storeFile=keystore/borg-release.jks
storePassword=change-me
keyAlias=borg-release
keyPassword=change-me
